<Window x:Class="FileExplorer.App.BatchRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Batch Rename" Width="760" Height="560"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBg}" Foreground="{DynamicResource TextFg}"
        FontFamily="Segoe UI" FontSize="13">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Find / Replace -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Find:" VerticalAlignment="Center"/>
            <TextBox x:Name="FindBox" Grid.Column="1" Margin="0,0,12,0"
                     TextChanged="OnSpecChanged"/>
            <TextBlock Grid.Column="2" Text="Replace:" VerticalAlignment="Center"/>
            <TextBox x:Name="ReplaceBox" Grid.Column="3"
                     TextChanged="OnSpecChanged"/>
        </Grid>

        <!-- Row 1: Options checkboxes -->
        <WrapPanel Grid.Row="1" Margin="0,8,0,0">
            <CheckBox x:Name="RegexCheck" Content="Use Regex" Foreground="{DynamicResource TextFg}" Margin="0,0,16,0"
                      Checked="OnSpecChanged" Unchecked="OnSpecChanged"/>
            <CheckBox x:Name="CaseCheck" Content="Case Sensitive" Foreground="{DynamicResource TextFg}" Margin="0,0,16,0"
                      Checked="OnSpecChanged" Unchecked="OnSpecChanged"/>
            <CheckBox x:Name="CounterCheck" Content="Add Counter" Foreground="{DynamicResource TextFg}" Margin="0,0,8,0"
                      Checked="OnSpecChanged" Unchecked="OnSpecChanged"/>
            <TextBlock Text="Start:" VerticalAlignment="Center" Margin="8,0,4,0"/>
            <TextBox x:Name="CounterStartBox" Width="40" Text="1" TextChanged="OnSpecChanged"/>
            <TextBlock Text="Step:" VerticalAlignment="Center" Margin="8,0,4,0"/>
            <TextBox x:Name="CounterStepBox" Width="40" Text="1" TextChanged="OnSpecChanged"/>
            <TextBlock Text="Pad:" VerticalAlignment="Center" Margin="8,0,4,0"/>
            <TextBox x:Name="CounterPadBox" Width="40" Text="1" TextChanged="OnSpecChanged"/>
        </WrapPanel>

        <!-- Row 2: Prefix / Suffix / Case / Extension -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Prefix:" VerticalAlignment="Center"/>
            <TextBox x:Name="PrefixBox" Grid.Column="1" Margin="0,0,12,0" TextChanged="OnSpecChanged"/>
            <TextBlock Grid.Column="2" Text="Suffix:" VerticalAlignment="Center"/>
            <TextBox x:Name="SuffixBox" Grid.Column="3" Margin="0,0,12,0" TextChanged="OnSpecChanged"/>
            <TextBlock Grid.Column="4" Text="Case:" VerticalAlignment="Center"/>
            <ComboBox x:Name="CaseCombo" Grid.Column="5" Margin="0,0,12,0"
                      SelectionChanged="OnSpecChanged">
                <ComboBoxItem Content="None" IsSelected="True"/>
                <ComboBoxItem Content="lower"/>
                <ComboBoxItem Content="UPPER"/>
                <ComboBoxItem Content="Title Case"/>
            </ComboBox>
            <TextBlock Grid.Column="6" Text="Ext:" VerticalAlignment="Center"/>
            <TextBox x:Name="ExtBox" Grid.Column="7" TextChanged="OnSpecChanged"/>
        </Grid>

        <!-- Row 3: Header -->
        <Border Grid.Row="3" Margin="0,12,0,0" Background="{DynamicResource HeaderBg}" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Original" FontWeight="SemiBold" Foreground="{DynamicResource SubTextFg}"/>
                <TextBlock Grid.Column="1" Text="→" Foreground="{DynamicResource SubTextFg}" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="New Name" FontWeight="SemiBold" Foreground="{DynamicResource SubTextFg}"/>
            </Grid>
        </Border>

        <!-- Row 4: Preview list -->
        <ListView x:Name="PreviewList" Grid.Row="4" Background="{DynamicResource PaneBg}" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Original" Width="320"
                                    DisplayMemberBinding="{Binding Original}"/>
                    <GridViewColumn Header="New Name" Width="320"
                                    DisplayMemberBinding="{Binding New}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Row 5: Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock x:Name="StatusLabel" VerticalAlignment="Center" Foreground="{DynamicResource SubTextFg}" Margin="0,0,16,0"/>
            <Button Content="Rename" Width="90" Height="30" Click="Rename_Click"
                    Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"
                    Cursor="Hand" FontWeight="SemiBold"/>
            <Button Content="Cancel" Width="80" Height="30" Click="Cancel_Click"
                    Background="{DynamicResource HeaderBg}" Foreground="{DynamicResource TextFg}" BorderThickness="0"
                    Cursor="Hand" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
